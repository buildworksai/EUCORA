# SPDX-License-Identifier: Apache-2.0
# Copyright (c) 2026 BuildWorks.AI
apiVersion: v1
kind: Service
metadata:
  name: eucora-web
  namespace: eucora
  labels:
    app: eucora-web
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: eucora-web
