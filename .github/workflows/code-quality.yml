# SPDX-License-Identifier: Apache-2.0
# Copyright (c) 2025 BuildWorks.AI

name: Code Quality & Compliance

on:
  push:
    branches: [ main, develop, feature/** ]
  pull_request:
    branches: [ main, develop ]

jobs:
  spdx-compliance:
    name: SPDX License Compliance
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup PowerShell
        uses: actions/setup-powershell@v1

      - name: Check SPDX Headers
        run: |
          pwsh -File scripts/utilities/Add-SPDXHeaders.ps1 -DryRun
          if [ $? -ne 0 ]; then
            echo "❌ SPDX compliance check failed"
            echo "Run 'pwsh -File scripts/utilities/Add-SPDXHeaders.ps1' to fix"
            exit 1
          fi
          echo "✅ All PowerShell files have SPDX headers"

  powershell-lint:
    name: PowerShell Linting
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup PowerShell
        uses: actions/setup-powershell@v1

      - name: Install PSScriptAnalyzer
        run: |
          pwsh -Command "Install-Module -Name PSScriptAnalyzer -Force -Scope CurrentUser"

      - name: Run PSScriptAnalyzer
        run: |
          pwsh -Command "
            \$results = Invoke-ScriptAnalyzer -Path . -Recurse -Settings PSGallery -Severity Error,Warning
            if (\$results) {
              \$results | Format-Table -AutoSize
              Write-Error 'PSScriptAnalyzer found issues'
              exit 1
            }
            Write-Host '✅ PSScriptAnalyzer passed with zero errors/warnings'
          "

  file-quality:
    name: File Quality Checks
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check for trailing whitespace
        run: |
          if grep -r --include="*.ps1" --include="*.md" --include="*.json" -n "[[:space:]]$" .; then
            echo "❌ Files with trailing whitespace found"
            exit 1
          fi
          echo "✅ No trailing whitespace"

      - name: Check for large files
        run: |
          large_files=$(find . -type f -size +5M -not -path "*/node_modules/*" -not -path "*/.git/*")
          if [ -n "$large_files" ]; then
            echo "❌ Large files found:"
            echo "$large_files"
            exit 1
          fi
          echo "✅ No large files"

      - name: Validate JSON files
        run: |
          for file in $(find . -name "*.json" -not -path "*/node_modules/*"); do
            if ! jq empty "$file" 2>/dev/null; then
              echo "❌ Invalid JSON: $file"
              exit 1
            fi
          done
          echo "✅ All JSON files valid"

      - name: Validate YAML files
        run: |
          pip install pyyaml
          for file in $(find . -name "*.yaml" -o -name "*.yml" -not -path "*/node_modules/*"); do
            if ! python -c "import yaml; yaml.safe_load(open('$file'))" 2>/dev/null; then
              echo "❌ Invalid YAML: $file"
              exit 1
            fi
          done
          echo "✅ All YAML files valid"

  branding-compliance:
    name: Branding Compliance
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check for old branding references
        run: |
          if grep -r --include="*.md" --include="*.ps1" -i "desktop-app-packaging" . --exclude-dir=".git" --exclude="CHANGELOG.md"; then
            echo "❌ Old branding references found"
            exit 1
          fi
          echo "✅ No old branding references"

      - name: Verify EUCORA branding
        run: |
          if ! grep -q "EUCORA" README.md; then
            echo "❌ README.md missing EUCORA branding"
            exit 1
          fi
          echo "✅ EUCORA branding present"

  summary:
    name: Quality Summary
    runs-on: ubuntu-latest
    needs: [spdx-compliance, powershell-lint, file-quality, branding-compliance]
    
    steps:
      - name: Report Success
        run: |
          echo "🎉 All quality checks passed!"
          echo "✅ SPDX Compliance"
          echo "✅ PowerShell Linting"
          echo "✅ File Quality"
          echo "✅ Branding Compliance"
